for (let i = 0; i < 10; i++) { //shortcut for selecting all occurence of a word is ctrl + F2
    const element = i;
    //console.log(element); 
}


let array = ['a', 'b', 'c']
for (let index = 0; index < array.length; index++) {
    const element = array[index];
    //console.log(array[index]);
}

//break and continue